<table class="table table-bordered">
  <tbody>
  <tr>
    <th>Backend</th>
    <td><%= link_to @backend.name, frontend_backend_path(@backend.frontend, @backend) %></td>
  </tr>
  <tr>
    <th>Hostnames</th>
    <td><%= @backend.hostnames.map {|x| "<div>#{x}</div>"}.join(" ").html_safe %></td>
  </tr>
  <tr>
    <th>Locations</th>
    <td><%= @backend.locations.map {|x| "<div>#{x}</div>"}.join(" ").html_safe %></td>
  </tr>
  </tbody>
</table>